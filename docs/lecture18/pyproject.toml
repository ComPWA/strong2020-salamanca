[project]
name = "lecture"
version = "0.1.0"
requires-python = "~=3.13.0"
dependencies = [
    "gdown",
    "matplotlib",
    "numpy",
]

[tool.ruff]
preview = true

[tool.ruff.lint]
ignore = [
    "COM812",
    "CPY001",
    "D",
    "ISC001",
    "PTH110",
]
select = ["ALL"]

[tool.uv.workspace]
